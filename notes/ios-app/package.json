{
  "name": "inkwell-ios",
  "version": "1.0.0",
  "private": true,
  "description": "The iOS wrapper for Inkwell. The app itself lives in ../ — this only packages it.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "ios:add": "npm run build && npx cap add ios && node scripts/patch-ios.mjs",
    "ios:sync": "npm run build && npx cap sync ios && node scripts/patch-ios.mjs",
    "ios:open": "npx cap open ios",
    "check": "node ../selftest.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/filesystem": "^8.0.0",
    "@capacitor/share": "^8.0.0",
    "pdfjs-dist": "3.11.174",
    "@fontsource/fraunces": "^5.3.0",
    "@fontsource/dm-sans": "^5.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
